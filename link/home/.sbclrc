#-quicklisp
(let ((quicklisp-init (merge-pathnames #p".roswell/lisp/quicklisp/setup.lisp"
                        (user-homedir-pathname))))
  (when (probe-file quicklisp-init)
    (load quicklisp-init)))
