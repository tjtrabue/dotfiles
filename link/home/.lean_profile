#!/bin/sh

# This file is a lean shell profile to be used under restricted circumstances.
# For instance, if you are on a low-powered computer and need a basic shell
# session without all of the overhead of my usual configuration, you can simply
# source this file from ~/.bash_profile or ~/.zshenv.

# File containing the $PATH variable.
STATIC_PATH="${HOME}/.path_static"

if [ -f "${STATIC_PATH}" ]; then
  . "${STATIC_PATH}"
else
  # IMPORTANT: This PATH may be overriden by the export_path function. This only
  # exists in order to make sure that certain necessary executables are found
  # before running the main shell profile logic.
  export PATH="${HOME}/workspace/flutter/bin:${HOME}/perl5/bin:${HOME}/go/bin:${HOME}/.asdf/bin:${HOME}/.roswell/bin:${HOME}/.rvm/gems/default/bin:${HOME}/.rvm/rubies/default/bin:${HOME}/.rvm/bin:${HOME}/.pyenv/shims:${HOME}/.pyenv/bin:${HOME}/.sdkman/candidates/maven/current/bin:${HOME}/.sdkman/candidates/kotlin/current/bin:${HOME}/.sdkman/candidates/java/current/bin:${HOME}/.sdkman/candidates/gradle/current/bin:${HOME}/.jenv/bin:${HOME}/.cargo/bin:${HOME}/.npm-global/bin:${HOME}/.luarocks/bin:${HOME}/.cabal/bin:${HOME}/.ghcup/bin:${HOME}/.fzf/bin:${HOME}/.dotfiles/bin:${HOME}/.local/bin:${HOME}/bin:/usr/local/Cellar/libtool/2.4.6_4/libexec/gnubin:/usr/local/Cellar/grep/3.7/libexec/gnubin:/usr/local/Cellar/gnu-which/2.21/libexec/gnubin:/usr/local/Cellar/gnu-tar/1.34/libexec/gnubin:/usr/local/Cellar/gnu-sed/4.8/libexec/gnubin:/usr/local/Cellar/gnu-indent/2.2.12_1/libexec/gnubin:/usr/local/Cellar/gawk/5.1.1/libexec/gnubin:/usr/local/Cellar/findutils/4.8.0_1/libexec/gnubin:/usr/local/Cellar/ed/1.17/libexec/gnubin:/usr/local/Cellar/coreutils/9.0/libexec/gnubin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin/vendor_perl:/usr/bin/core_perl:/usr/bin:/sbin:/bin"
fi

unset STATIC_PATH

# vim:foldenable:foldmethod=marker:foldlevel=0
