#!/bin/sh

# Start `iex` and load the current project's modules.
alias iexm="iex -S mix"

# Start Phoenix server interactively in `iex`.
alias iphx="iex -S mix phx.server"

# Run `mix` in the prod environment.
alias pmix="MIX_ENV=prod mix"

# vim:foldenable:foldmethod=marker:foldlevel=0
