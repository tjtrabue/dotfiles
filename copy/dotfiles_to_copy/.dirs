#!/bin/sh

# Directory alias file.
# Aliases are added to this file via the "diralias" command.
dot="${HOME}/.dotfiles"
conf="${HOME}/.config"
extra="${HOME}/.extra"
emd="${HOME}/.emacs.d"
nv="${conf}/nvim"
ws="${HOME}/workspace"
prac="${HOME}/practice"
apps="${HOME}/applications"
arch="${apps}/archives"
dl="${HOME}/Downloads"
packer="${PYTHONUSERBASE}/share/nvim/site/pack/packer/start"
straight="${emd}/straight/repos"
