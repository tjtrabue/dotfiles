#!/usr/bin/env bash

# Directory alias file.
# Aliases are added to this file via the "diralias" command.
dot="${HOME}/.dotfiles"
emd="${HOME}/.emacs.d"
ws="${HOME}/workspace"
prac="${ws}/practice"
apps="${HOME}/applications"
arch="${apps}/archives"
dl="${HOME}/Downloads"
